// let regexString = new RegExp(/#(?:[a-f\d]{6})/, 'gi');
// let testStr = '#7d3b0c';
// console.log(testStr.trim().match(regexString));
// console.log(regexString.exec(testStr));

let arr = [];

arr.pop();

console.log(arr);
